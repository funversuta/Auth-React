import styled from 'styled-components';

export const Head = styled.form `
   display: flex; 
   text-align: center;
   flex-direction: row;
   max-width: 200px;
   margin: 20px auto;
   padding-top: 300px;
   font-size: 40px;
   font-weight: bold;
   `;
